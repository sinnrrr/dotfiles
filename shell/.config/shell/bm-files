# These files automatically update when edited/saved in vim:

# keys	filename				description
bf	${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-files	# This file, a list of bookmarked files
bd	${XDG_CONFIG_HOME:-$HOME/.config}/shell/bm-dirs		# A list of bookmarked directories similar to this file

# These do not update automatically, but on the next new instance of a program:

cz	$ZDOTDIR/.zshrc						# zsh (shell) config
ca	${XDG_CONFIG_HOME:-$HOME/.config}/shell/aliasrc		# aliases used by zsh (and potentially other shells)
cp	${XDG_CONFIG_HOME:-$HOME/.config}/shell/profile		# profile file for login settings for zsh
cl	${XDG_CONFIG_HOME:-$HOME/.config}/lf/lfrc	
ce  ${XDG_CONFIG_HOME:-$HOME/.config}/lvim/config.lua
cep ${XDG_CONFIG_HOME:-$HOME/.config}/lvim/lua/user/plugins.lua
ct	${XDG_CONFIG_HOME:-$HOME/.config}/alacritty/alacritty.yml
cy  ${XDG_CONFIG_HOME:-$HOME/.config}/yabai/yabairc
cs  ${XDG_CONFIG_HOME:-$HOME/.config}/skhd/skhdrc
